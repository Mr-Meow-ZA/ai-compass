{
  "name": "ai-compass",
  "version": "0.6.0",
  "private": true,
  "description": "AI education, discovery and reference platform",
  "scripts": {
    "serve": "python -m http.server 4173",
    "check": "node --check app.js && node --check data.js && node --check content.js"
  }
}
